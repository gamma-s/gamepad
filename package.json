{
  "name": "gamepad",
  "version": "1.0.0",
  "description": "An interface that makes the Gamepad api in the browser easier to use",
  "main": "./src/index.js",
  "scripts": {
    "lint": "gts lint",
    "clean": "gts clean",
    "compile": "tsc",
    "fix": "gts fix",
    "prepare": "npm run compile",
    "pretest": "npm run compile",
    "posttest": "npm run lint"
  },
  "keywords": [
    "Gamepad",
    "Controller"
  ],
  "author": "",
  "license": "GPL-3.0-or-later",
  "devDependencies": {
    "gts": "^3.1.1",
    "typescript": "~4.7.0",
    "@types/node": "^14.11.2"
  }
}
